---
description: Project long-term memory router (ai-memory handoff/search/save)
argument-hint: "[status|handoff|search <q>|save ...]"
---

# /memory

Load skill `ai-memory`.

| Args | Action |
|------|--------|
| empty / help | Preflight + short how-to; offer handoff if resuming |
| `status` | `/memory-status` |
| `handoff` | `/memory-handoff` |
| `search ...` | `/memory-search` |
| `save ...` | `/memory-save` |
